% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Heatmap_functions.R
\name{heatmap_from_eset}
\alias{heatmap_from_eset}
\title{Prepare Heatmap Data from ExpressionSet}
\usage{
heatmap_from_eset(
  eset,
  assay_name = "exprs",
  sample_cols = NULL,
  feature_cols = NULL,
  sample_indices = NULL,
  feature_indices = NULL
)
}
\arguments{
\item{eset}{ExpressionSet object from Biobase}

\item{assay_name}{Character; name of assay to extract (default "exprs")}

\item{sample_cols}{Character vector; pData column names for sample annotations}

\item{feature_cols}{Character vector; fData column names for feature annotations (row annotations)}

\item{sample_indices}{Integer vector; indices of samples to include (default all)}

\item{feature_indices}{Integer vector; indices of features to include (default all)}
}
\value{
List with elements:
\describe{
\item{m}{Numeric matrix of expression data (features x samples)}
\item{sample_meta}{Data frame of sample annotations for heatmap columns}
\item{feature_meta}{Data frame of feature annotations for heatmap rows}
\item{sample_anno_col}{Named list of colors for sample annotations}
\item{feature_anno_col}{Named list of colors for feature annotations}
\item{rows}{Number of features (rows)}
\item{cols}{Number of samples (columns)}
}
}
\description{
Extract and prepare data from an ExpressionSet object for heatmap visualization.
Supports filtering of samples and features, and extraction of annotations from
both pData (sample annotations) and fData (feature annotations).
}
\note{
Version 1.0 - Created for ExpressionSet-based heatmap module
Requires properly structured ExpressionSet with pData and optionally fData
}
\examples{
\dontrun{
library(Biobase)
data(sample. ExpressionSet)

# Basic usage
hm_data <- heatmap_from_eset(
  sample.ExpressionSet,
  assay_name = "exprs",
  sample_cols = c("type", "score"),
  feature_cols = NULL
)

# With filtering
hm_data <- heatmap_from_eset(
  sample.ExpressionSet,
  assay_name = "exprs",
  sample_cols = c("type"),
  feature_cols = c("Chromosome"),
  sample_indices = 1:10,
  feature_indices = 1:50
)
}

}
