% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudoarray_functions.R
\name{get_pseudo_array_scale}
\alias{get_pseudo_array_scale}
\title{Get Pseudo Array Color Scale Limits with Multiple Modes}
\usage{
get_pseudo_array_scale(
  sample,
  datCollate,
  scale_type = "RFU",
  scale_mode = c("auto_per_plot", "auto_global", "custom"),
  custom_min = NULL,
  custom_max = NULL
)
}
\arguments{
\item{sample}{Character string specifying the sample name to analyze}

\item{datCollate}{List object containing experiment data}

\item{scale_type}{Character string specifying scale type: "RFU" or "Log2"}

\item{scale_mode}{Character string specifying scaling strategy:
\itemize{
\item "auto_per_plot": No limits (ggplot2 auto-scales each plot independently) - ORIGINAL BEHAVIOR
\item "auto_global": Calculate global min/max from all data, apply to all plots
\item "custom": Use user-specified custom_min and custom_max
Default is "auto_per_plot"
}}

\item{custom_min}{Numeric value for custom minimum scale limit. Only used if
scale_mode = "custom". Default is NULL}

\item{custom_max}{Numeric value for custom maximum scale limit. Only used if
scale_mode = "custom". Default is NULL}
}
\value{
A list with four elements:
\item{min}{Minimum value for the color scale (NULL for auto_per_plot)}
\item{max}{Maximum value for the color scale (NULL for auto_per_plot)}
\item{type}{The scale type used ("RFU" or "Log2")}
\item{mode}{The scale mode used}
}
\description{
Calculates appropriate color scale limits for pseudo array plots with support
for multiple scaling strategies: auto-scale per plot, global auto-scale, or
explicit custom limits.
}
\details{
\strong{Scaling Modes:}
\enumerate{
\item \strong{auto_per_plot} (ORIGINAL - Maximum contrast per plot):
\itemize{
\item Returns NULL for min/max
\item ggplot2 auto-scales each plot to its own data range
\item Pros: Maximum detail/contrast in each individual plot
\item Cons: Colors NOT comparable between plots
\item Use when: Examining single samples in isolation
}
\item \strong{auto_global} (Consistent auto-scaling):
\itemize{
\item Calculates min/max from all data for this sample
\item Same scale applied to BG, FG, and NetI plots
\item Pros: Colors comparable within a sample's trio of plots
\item Cons: May lose some detail if ranges differ greatly
\item Use when: Comparing BG/FG/NetI within one sample
}
\item \strong{custom} (Match TIFF viewer):
\itemize{
\item Uses explicit user-provided min/max
\item Pros: Perfect match to TIFF image viewer colors
\item Cons: Requires knowing appropriate limits
\item Use when: Comparing pseudo arrays to TIFF images
}
}
}
\author{
DrGarnett
}
