% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/denoiser_processing.R
\name{label_candidate_cutpoints}
\alias{label_candidate_cutpoints}
\title{Label Candidate Cutpoints per PC Level}
\usage{
label_candidate_cutpoints(cutpoint_results, exp_PN_AAbs, cut_step)
}
\arguments{
\item{cutpoint_results}{Data frame; output from denoise_find_cutpoints() with
PCs_removed column added. Must contain columns: cutpoint, zz_2_frac,
zz_4_frac, TP_FP_ratio, PN_Aab_count_67_perc, PN_AAb_hit_rate, PCs_removed}

\item{exp_PN_AAbs}{Numeric vector; expected range of PN AAb counts (e.g. 4:8)}

\item{cut_step}{Numeric; increment used in cut_seq (e.g. 0.1)}
}
\value{
Data frame with labelled candidate rows and a \code{label} column in the
format "PC1_a", "PC1_2_b", "PC1to4_d" etc. Returns NULL if no valid
candidates found.
}
\description{
Identifies the four candidate cutpoints (a, b, c, d) for each PC removal
level in the cutpoint results table, mirroring the logic in cut_and_drying().
}
\details{
Candidate definitions:
\itemize{
\item b: one step below the first cutpoint where BOTH zz_2_frac AND zz_4_frac == 0
\item a: one step below b
\item c: one step above b
\item d: cutpoint with highest TP:FP ratio where PN_Aab_count_67_perc is in
exp_PN_AAbs AND PN_AAb_hit_rate >= 65. Falls back to max TP:FP overall
if no candidates meet criteria.
}
}
\note{
Original logic from cut_and_drying() in Sengenics_accessory_functions.R
}
\examples{
\dontrun{
s_df <- label_candidate_cutpoints(
  cutpoint_results = rv$cutpoint_results,
  exp_PN_AAbs = seq(input$exp_PN_min, input$exp_PN_max),
  cut_step = input$cut_step
)
}
}
