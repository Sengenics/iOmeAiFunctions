% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pre-Processing functions.R
\name{lowIntensityFilterFunction}
\alias{lowIntensityFilterFunction}
\title{Low-intensity filter (nMAD = 2) for feature RFU}
\usage{
lowIntensityFilterFunction(
  datCollate,
  feature_label = "feature",
  test = FALSE,
  sample_select = NULL
)
}
\arguments{
\item{datCollate}{A list with \code{datCollate$data$Data} (data.frame) containing
at least columns: \code{Sample}, \code{data}, \code{mean}, \code{source}. \code{datCollate$data$feature_df}
may be present but is not required for this step.}

\item{feature_label}{Character. The label in column \code{data} that designates
feature rows. Default is \code{"feature"}.}

\item{test}{Logical. If \code{TRUE}, builds a set of diagnostic ggplot objects
(returned invisibly via an attribute). Default \code{FALSE}.}

\item{sample_select}{Optional character. Sample to highlight in diagnostics
when \code{test = TRUE}. Defaults to the first sample in the data.}
}
\value{
The input \code{datCollate} with:
\itemize{
\item \code{datCollate$data$Data} — updated with threshold columns and \code{source} flagged as \code{"lowNetI"} where applicable
\item \code{datCollate$data$lowIntensityThresholds} — per-sample summary and thresholds
}
}
\description{
Computes per-sample log2 NetI summaries on feature rows, defines a low-intensity
RFU cutoff as median − 2 × MAD (on the log2 scale), converts that cutoff back
to RFU, and flags rows with \code{mean} below the cutoff by setting \code{source = "lowNetI"}.
}
\note{
Version 1.0.0 from
Pre-Processing functions.R
}
\examples{
\dontrun{
  datCollate <- lowIntensityFilterFunction(datCollate)
}

}
