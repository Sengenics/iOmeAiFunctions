% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Heatmap_functions.R
\name{heatmap_eset_module_server}
\alias{heatmap_eset_module_server}
\title{Heatmap ExpressionSet Module Server}
\usage{
heatmap_eset_module_server(
  id,
  eset,
  assay_name,
  sample_filter,
  feature_filter,
  sample_anno_cols,
  feature_anno_cols,
  mode = "basic",
  features = list()
)
}
\arguments{
\item{id}{Character; module namespace ID}

\item{eset}{Reactive ExpressionSet object}

\item{assay_name}{Reactive character; which assay to visualize}

\item{sample_filter}{List from mod_data_filter_server for sample filtering}

\item{feature_filter}{List from mod_data_filter_server for feature filtering}

\item{sample_anno_cols}{Reactive character vector; pData columns to annotate}

\item{feature_anno_cols}{Reactive character vector; fData columns to annotate (row annotations)}

\item{mode}{Character or reactive; "basic" or "advanced" (default "basic")}

\item{features}{List or reactive; feature flags for future enhancements}
}
\value{
Reactive expression returning heatmap data list
}
\description{
Server module for ExpressionSet-based heatmap with multi-level filtering,
sample and feature annotations, and mode/feature flag support.
}
\note{
Version 1.0 - Created for ExpressionSet-based heatmap module
Supports both sample (column) and feature (row) annotations
}
\examples{
\dontrun{
# In server
sample_filter <- mod_data_filter_server("sample_filter", eset, "pData")
feature_filter <- mod_data_filter_server("feature_filter", eset, "fData")

heatmap_eset_module_server(
  "heatmap",
  eset = reactive(my_eset),
  assay_name = reactive("exprs"),
  sample_filter = sample_filter,
  feature_filter = feature_filter,
  sample_anno_cols = reactive(c("Sample_Group", "Disease_State")),
  feature_anno_cols = reactive(c("Protein_Family")),
  mode = "basic",
  features = list()
)
}

}
