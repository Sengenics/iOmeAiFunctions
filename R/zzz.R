
.onLoad <- function(libname, pkgname) {
  packages_to_load <-  AUC 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  callr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  caret 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  caret 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  circlize 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  cluster 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  cluster 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  corrplot 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  data.table 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  doParallel 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  dplyr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  DT 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  flextable 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  foreach 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  ggforce 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  ggplot2 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  ggplotify 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  ggpubr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  ggrepel 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  glmnet 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  greekLetters 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  gridExtra 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  gridExtra 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  gtools 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  hilbertSimilarity 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  Hmisc 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  kableExtra 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  lubridate 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  magick 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  markdown 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  markdown 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  naniar 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  officer 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  openxlsx 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  OptimalCutpoints 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  paletteer 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  pals 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  parallel 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  PerformanceAnalytics 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  pheatmap 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  plotly 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  plotROC 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  plyr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  pROC 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  pspline 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  psych 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  purrr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  randomForest 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  RColorBrewer 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  readr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  readtext 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  reshape 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  ROCR 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  ROCR 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  Rtsne 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  Rtsne 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  rvg 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinyBS 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinyBS 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinybusy 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinycssloaders 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinydashboard 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinydashboard 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinydisconnect 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinydisconnect 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinyFiles 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinyjs 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinyjs 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinyWidgets 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shinyWidgets 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  stringr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  tidyr 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  tidyverse 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  uuid 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  vegan 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  vegan 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  viridis 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  writexl 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  rlang 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  shiny 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  correlationfunnel 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  pcaMethods 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  imputeLCMD 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  limma 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  EnhancedVolcano 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  sva 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  metagenomeSeq 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}


.onLoad <- function(libname, pkgname) {
  packages_to_load <-  jpeg 

  for (pkg in packages_to_load) {
    if (requireNamespace(pkg, quietly = TRUE)) {
      suppressPackageStartupMessages(
        library(pkg, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)
      )
    }
  }
}

